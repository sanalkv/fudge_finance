import 'package:flutter/painting.dart';

//Padding
const defaultPadding = EdgeInsets.all(20);
const topPadding = EdgeInsets.only(top:20);
const bottomPadding = EdgeInsets.only(bottom:20);
const leftPadding = EdgeInsets.only(left:20);
const rightPadding = EdgeInsets.only(right:20);

