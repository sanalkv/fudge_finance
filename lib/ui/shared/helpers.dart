import 'package:flutter/painting.dart';

//Padding
const defaultPadding = EdgeInsets.all(23);
const topPadding = EdgeInsets.only(top:23);
const bottomPadding = EdgeInsets.only(bottom:23);
const leftPadding = EdgeInsets.only(left:23);
const rightPadding = EdgeInsets.only(right:23);

